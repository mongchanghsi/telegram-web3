import Button from "@/components/Shared/Button";
import { BaseViewContainer } from "../BaseView";

const DrumView = () => {
  return (
    <BaseViewContainer>
      <Button label="Drum" />
    </BaseViewContainer>
  );
};

export default DrumView;
